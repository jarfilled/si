name: monitor_channels
description: Background-safe channels for S Health.
version: 1.0.0
publish_to: 'none'
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.s_health.monitor_channels
        pluginClass: SHealthMonitorPlugin
